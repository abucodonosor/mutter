Index: mutter-3.6.2/configure.ac
===================================================================
--- mutter-3.6.2.orig/configure.ac
+++ mutter-3.6.2/configure.ac
@@ -411,11 +411,6 @@ if test "$enable_compile_warnings" != no
     esac
 
     case " $CFLAGS " in
-    *[\ \	]-Wcast-align[\ \	]*) ;;
-    *) CFLAGS="$CFLAGS -Wcast-align" ;;
-    esac
-
-    case " $CFLAGS " in
     *[\ \	]-Wsign-compare[\ \	]*) ;;
     *) CFLAGS="$CFLAGS -Wsign-compare" ;;
     esac
